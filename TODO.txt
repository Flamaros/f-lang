Mettre en place un logger avec un mecanisme de gestion des erreurs:
 * Certaines erreurs comme le fait de redefinir une valeur d'un enum ne sont pas critique d'un point de vu syntaxique, donc pas besoin de stopper immédiatement la compilation (on peut laisser plus d'erreurs apparaitre pour l'utilisateur)
 * Certaines erreurs sont critique et la compilation ne peut absolument plus continuer
 